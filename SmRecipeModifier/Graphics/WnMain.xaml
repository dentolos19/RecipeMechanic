<mahApps:MetroWindow x:Class="SmRecipeModifier.Graphics.WnMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mahApps="http://metro.mahapps.com/winfx/xaml/controls"
        Title="Scrap Mechanic Recipe Modifier" SaveWindowPosition="True" ResizeMode="CanResizeWithGrip">
    <Window.CommandBindings>
        <CommandBinding Command="Open" Executed="Open"/>
        <CommandBinding Command="Save" Executed="Save"/>
        <CommandBinding Command="SaveAs" Executed="SaveAs"/>
    </Window.CommandBindings>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="Open" InputGestureText="Ctrl+O" Command="Open"/>
                <Separator/>
                <MenuItem Header="Save" InputGestureText="Ctrl+S" Command="Save"/>
                <MenuItem Header="Save As" InputGestureText="Ctrl+Shift+S" Command="SaveAs"/>
                <Separator/>
                <MenuItem Header="Exit" InputGestureText="Alt+F4" Click="Exit"/>
            </MenuItem>
            <MenuItem Header="Backup">
                <MenuItem Header="Create Backup" Click="CreateBackup"/>
                <MenuItem Header="Apply Backup" Click="ApplyBackup"/>
            </MenuItem>
            <MenuItem Header="Options">
                <MenuItem Header="Preferences" Click="ShowPreferences"/>
                <Separator/>
                <MenuItem Header="About SmRecipeModifier" Click="ShowAbout"/>
            </MenuItem>
        </Menu>
        <DockPanel>
            <StackPanel Width="150">
                <Image Margin="10,10,10,0" Source="../Resources/Assets/Logo.png" Height="130"/>
                <Separator Margin="10,10,10,0"/>
                <TextBox x:Name="FileNameBox" Margin="10,10,10,0" HorizontalContentAlignment="Center" IsReadOnly="True"/>
                <Button Margin="10,10,10,0" Content="Open" Command="Open"/>
                <Button Margin="10,10,10,0" Content="Save" Command="Save"/>
                <Button Margin="10,10,10,0" Content="Save As" Command="SaveAs"/>
                <Separator Margin="10,10,10,0"/>
                <Button Margin="10,10,10,0" Content="Add Recipe" Click="AddRecipe"/>
                <Button Margin="10,10,10,0" Content="Remove Recipe" Click="RemoveRecipe"/>
                <Button Margin="10,10,10,0" Content="Modify Recipe" Click="ModifyRecipe"/>
                <Separator Margin="10,10,10,0"/>
                <Label Margin="10,10,10,0" Content="Powered by .NET 5" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
            </StackPanel>
            <TabControl>
                <TabItem Header="Modify Recipes">
                    <DockPanel>
                        <TextBlock x:Name="RecipeListItemAmountText" DockPanel.Dock="Top" Text="Open a file to get started modifying your way!"/>
                        <ListView x:Name="RecipeList" MouseDoubleClick="ModifyRecipe">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Name" Width="250" DisplayMemberBinding="{Binding Name}"/>
                                    <GridViewColumn Header="In-Game Name" Width="250" DisplayMemberBinding="{Binding InGameName}"/>
                                    <GridViewColumn Header="ID" Width="300" DisplayMemberBinding="{Binding Id}"/>
                                </GridView>
                            </ListView.View>
                            <ListView.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Copy ID" Click="CopyRecipeId"/>
                                </ContextMenu>
                            </ListView.ContextMenu>
                        </ListView>
                    </DockPanel>
                </TabItem>
                <TabItem Header="Item List">
                    <DockPanel>
                        <TextBlock x:Name="ItemListItemAmountText" DockPanel.Dock="Top"/>
                        <ListView x:Name="ItemList">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Name" Width="250" DisplayMemberBinding="{Binding Name}"/>
                                    <GridViewColumn Header="In-Game Name" Width="250" DisplayMemberBinding="{Binding InGameName}"/>
                                    <GridViewColumn Header="ID" Width="300" DisplayMemberBinding="{Binding Id}"/>
                                </GridView>
                            </ListView.View>
                            <ListView.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Copy ID" Click="CopyItemId"/>
                                </ContextMenu>
                            </ListView.ContextMenu>
                        </ListView>
                    </DockPanel>
                </TabItem>
            </TabControl>
        </DockPanel>
    </DockPanel>
</mahApps:MetroWindow>